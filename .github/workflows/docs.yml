name: Docs
on:
  pull_request:
    paths:
      - '**.md'
  push:
    branches:
      - main
    paths:
      - '**.md'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Markdown only, build skipped"
